- Fix email form width on shrinking window.

- Write a script to parse dates so if date of show passes, pull the next one to hero.

- Each band selection should be an about for that specific band.

- Note: Maybe keep shows a static length incl old shows, but upon adding new shows, remove old shows.
    That way, shows list remains a consistent display but dynamic adding is allowed.